# Production Readiness Orchestration State

This file is the tracked handoff for the production-readiness program. Update it after every merge and before handing work to another session. The `.facts` file remains the product contract; this file records execution state only. Ignored state under `.workflow/satelle-production-readiness/` is stale history and is not an operational authority.

## Frozen MVP Plan Checkpoint - 2026-07-20

- Authority: `satelle-mvp-pr-plan.md` and `satelle-mvp-pr-plan.json`; 30 exact PR packets cover the 774-fact planning baseline exactly once.
- Current merged main: `418072c7551ea4934fecdda62a2c968c589d813e` from PR #147.
- Frozen-plan progress: 112 of 774 baseline facts have been implemented since the planning baseline; 662 remain (`facts list --tags 'mvp and spec and not implemented'`).
- Formal packet progress: PRs 01-04 of 30 are merged. The serial contract-freeze gate is complete, and Wave 2 Local/Host vertical-slice work is eligible.
- PR #143 merged exact PR 01 membership at head `e3636d7e549604f2a4df4a64ecab2d422fec1a83`. All hosted checks passed, CodeRabbit findings were fixed and all three threads resolved, Greptile reviewed the final head successfully, and Codex review reported unavailable due account limits.
- PR #145 merged exact PR 02 membership at head `b58dacb553975eb6394b9ed36a47ffe427ab45f7`. The required Ubuntu merge gate, CodeRabbit, Greptile, and Codex reviews passed on the final head; all 19 review threads were resolved. Informational macOS quality and Windows ARM64 build failures remain deferred to the release gate.
- PR #146 merged exact PR 03 membership at head `bcee7214bf8b31ea0e2b145719fa7353a4f880c1` as merge commit `f215f51be9ae0f7ff4863caaf89ff3e3ce54946c`. The required Ubuntu merge gate, Windows Rust, docs, all npm platforms, CodeRabbit, Greptile, and Codex final-head review passed; all review threads were resolved. The known macOS Rust failure remained informational under the intermediate-merge policy.
- PR #147 merged exact PR 04 membership at head `99035b6fe59ddf8c3dc497f25eacd8d031000c16` as merge commit `418072c7551ea4934fecdda62a2c968c589d813e`. The required Ubuntu merge gate, Ubuntu and Windows Rust, docs, all npm platforms, CodeRabbit, Greptile, and Codex final-head review passed; the sole Codex thread was fixed and resolved. The known macOS Rust failure remained informational under the intermediate-merge policy.
- Earlier merged reductions preserved in the frozen-plan accounting: PR #140 contributed 19 baseline facts and PR #141 contributed 18; PR #143 contributed 24 new reductions because `sdt` was already implemented; PR #146 contributed 20 new reductions because 13 of its 33 exact facts were already implemented; PR #147 contributed 9 new reductions because 9 of its 18 exact facts were already implemented.
- PR 05, `Harden Bootstrap Lock recovery and the remote Host-binary cache`, is blocked on `GAP-017` and `GAP-018`. Its branch `feat/bootstrap-lock-binary-cache` proves and tags 17 of 21 assigned facts; `n2p`, `cwe`, `42a`, and `tf4` remain untagged. The implementation and focused gates are green, but the packet must not merge until reachable Host replacement ownership and portable mutation fencing/reconciliation are resolved.
- PR 08, `Prompt sources, limits, and timeout handling`, is the only packet whose frozen dependencies do not include PR 05. Its preserved worktree `/home/ubuntu/workspace/remoteuse-pr08-prompts` is a partial snapshot on `feat/prompt-sources-timeouts`; serialize integration because it overlaps PR 05 in `crates/satelle-core/src/lib.rs`.
- GAP-013 through GAP-016 are resolved by the previously selected PR 03 contracts: executable aggregate JSON results carry top-level schema versions, admission cancellation reuses the authenticated idempotency request identity, readiness and optional Goal metadata remain embedded Satelle concepts, and exact mutation protocol version `4` covers the incompatible safety changes.
- All 33 exact PR 03 facts are tagged implemented and merged.
- Preserved mixed work must not be opened as-is: `snapshot/frozen-plan-realign-bootstrap-mixed` at `97a7d7dcea9fedd624873059aab19126ba4d47b1`, `feat/release-promotion-contracts` at `12741d3d8db7b330a9984952d7da94b38ff1062e`, and closed PR #142 head `d7ddd89fdc2fb6c2fd1be56b029a37c9dc2b947d`.
- Resource policy: use one working tree and per-lane Cargo targets. Contract-freeze work uses `/home/ubuntu/workspace/remoteuse/target-contract-freeze`; no shared root target and no shared `flock`. Park one worker if RAM approaches 10 GiB.
- `.facts` SHA-256 on merged PR 04 main: `d90f1b94f4d0cbceb58d95149cca172069113137782957988f933de204c710cf`.

## Live Implementation Checkpoint - 2026-07-18

- Packet base: merged `main` at `76fc1783f228b006db623d128c51c783224c47ab` (PR #123).
- PRs #85 through #123 moved the program from verification-only work into feature implementation across bridge, storage, setup, config, sessions, distribution, doctor, documentation, CLI error boundaries, stop output, and release artifact ownership.
- PR #123 merged release artifact ownership and the native-package TOCTOU fix with its three exact fact transitions.
- Before this MCP PR, merged `main` has exactly 827 of 1,779 MVP facts implemented, with 952 remaining. This PR adds exactly six pre-existing MCP implementation tags, producing 833 implemented and 946 remaining.
- Active serial merge queue: MCP install on `feat/mcp-install` with exact facts `0ob`, `2ri`, `8f2`, `xl2`, `bb0`, and `wmq`.
- Tailscale source-complete batch: `n4h`, `5f3`, `fyf`, and `x4o` at change `xqlvvklq`, commit `8a8300a3617c`, in `remoteuse-tailscale-serve`; Cargo, facts, and publish are pending, and `feat/project-config-validation` still requires a semantic bookmark rename.
- Preserved source lanes: Maintenance Lease `vzyzrtmo` (`bfdc747e0907`) is blocked on production integration; Doctor scheduler `wrolxqxr` (`473feaf1cb67`) awaits live registration; setup retention `rrpsqlsk` (`f5e37587ef91`) is verified and PR-ready; trusted proxy `vvtlovzs` (`b13cafc1cdf1`) has functional tests green with contract/header grammar and Clippy pending.
- Preserved source lanes continued: Operator Log `xtxouvyx` (`26851a0c9279`) is source-complete with review pending; Desktop Binding `wymqvyvz` (`40f4057122e8`) is source-complete with public core errors/config wiring pending; Storage Restore `qutusrun` (`7f76e71bfa05`) is source-complete with Maintenance Lease, CLI, and restart wiring pending.
- MCP install has 29 installer tests and eight CLI contract/history tests. The previous exact head passed 123 CLI binary tests, 302 Host library tests with one fixture ignored, and the Windows GNU CLI/Host cross-check on the PR #123 base. Final review deltas pass focused CLI profile-source and project-isolation regressions, core invocation-profile validation, and the Host EINTR-deadline regression, along with package formatting and warnings-denied core, CLI, and Host Clippy.
- Active packet: publish, review, and serially merge the MCP installer.
- Final fact sheet SHA-256 with only the six exact pre-existing MCP implementation tags (`0ob`, `2ri`, `8f2`, `xl2`, `bb0`, `wmq`): `b180def05115207a2070318534db5ec2b131e8b1afb26b904956f14c9e3f8bd0`.
- The freeze inventory, ownership table, merge ledger, phase gates, and queue below are retained as historical evidence through PR #84. They are not the current work queue; live `main`, `.facts`, and this checkpoint take precedence.

## Freeze Baseline

- Phase: 1 - verification harness
- Latest main: `c6201a91713908436a8f845dce180fdf36ff9427`
- Freeze base: `a4bbea5a3aaf7df22c175587be26d072f25cfd03`
- Fact sheet SHA-256: `c95b7090372cc107bed9fb4752469fff4ae7798d945563f91aa57e5ab2fcb03c`
- Facts CLI: `facts 0.6.0`
- Fact write policy: do not add facts, contracts, or scope. Phase 1 may add executable command mappings to existing verification facts. Record undefined behavior in `.spec-gaps.md` instead of inventing requirements.

## Frozen Inventory

| Set | Total | Remaining |
| --- | ---: | ---: |
| All facts | 2,101 | 1,465 specs |
| Implemented | 636 | - |
| Spec | 1,703 | 1,465 |
| Draft | 0 | 0 |
| Contract | 223 | 71 |
| Verification | 38 | 37 |
| Release gate | 23 | 23 |
| MVP | 1,783 | 1,151 |
| Later | 318 | 314 |

At the freeze baseline, `@spec` and `@implemented` overlapped on 238 facts, remaining specs consisted of 1,151 MVP facts and 314 later facts, and tag totals included nine mapping-form `tags: [...]` entries. Current Phase 1 totals are recomputed below.

Current Phase 1 state: 661 implemented facts, 1,440 specs remaining, 67 contract facts remaining, 29 verification facts remaining, 20 release gates remaining, 1,120 MVP facts remaining, and 320 later facts remaining. All 20 remaining release gates also carry `@verification`, so the remaining verification and release-gate union is 29 facts. Fact `3ha` returned to `@spec` after review found that its command exercised a validator implemented entirely inside the test instead of a production release boundary. Six token-administration facts moved from MVP to later in `fdaf6cd`; no product implementation was removed.

## Current Ownership

| Lane | Owner | Active work | Unmerged branch |
| --- | --- | --- | --- |
| Orchestration | Root session | Phase 1 checkpoint and serial merge queue | `test/maintenance-no-mutation-contract` |
| Contract writer | Unassigned | Shared parser and semantic error assertions (`zox`) merged in PR #68 | None |
| Test migration | Unassigned | Flat-envelope CLI assertion migration merged in PR #65 | None |
| Archive harness integration | Unassigned | Shared semantic release archive case catalog and false `3ha` completion correction merged in PR #79 | None |
| Active Turn stop proof | Unassigned | Deterministic `hav` CLI proof merged in PR #80 | None |
| npm promotion harness | Unassigned | Shared semantic promotion transaction catalog merged in PR #81; all GAP-012 facts remain unmapped | None |
| Archive-to-npm parity harness | Unassigned | Shared executable digest-parity catalog merged in PR #82; `h9h` remains unmapped | None |
| Versioned payload harness | Unassigned | Shared channel-neutral JSON, NDJSON, field, enum, and carrier catalog merged in PR #83; `4ze` remains unmapped | None |
| Privacy canary harness | Unassigned | Shared raw-byte assertion and real CLI, HTTP, WebSocket, Host, and storage proofs merged in PR #84; `a5u` remains unmapped | None |
| Maintenance no-mutation harness | Root session | Linux and native Windows 11 watcher proof complete; PR review gates pending while `mg1` remains unmapped | `test/maintenance-no-mutation-contract` |
| Lane A bridge writer | Unassigned | Public-boundary liveness and WebSocket isolation (`kyc`, `q82`) merged in PR #71 | None |
| A: host and runtime | Unassigned | Terminal-state race invariants (`hjz`, `t10`, `sp8`) merged in PR #78 | None |
| B: CLI and configuration | Unassigned | Blocked by Phases 1 and 2 | None |
| C: release and distribution | Unassigned | Blocked by Phases 1 and 2 | None |

Only the listed file owners may edit, and concurrent writers must own disjoint files. Preparation and review lanes are read-only, and `.facts` remains untouched unless one explicit writer is assigned.

## Merge Ledger

- #54 macOS process cleanup and storage lock work: `30b943bf`
- #56 retain Codex stdout through cleanup: `1135029a`
- #55 npm, pnpm, and Bun one-shot distribution facts: `c2f8d7d7`
- #57 validate storage integrity before migrations: `2f0b3d61`
- #58 event control context validation: `a4bbea5a`
- #59 production-readiness fact freeze and orchestration checkpoint: `57435efd`
- #60 map verification facts to executable checks: `477d5648`
- Separate authorized session resolved GAP-001 and GAP-002: `fdaf6cde`
- Separate authorized session updated this ledger for those resolutions: `db22843f`
- #61 deterministic runtime synchronization and macOS group reaping: `d0ec268e`
- #62 cross-process Control Lease exclusion: `1b8eb4fa`
- #63 required-nullable core wire fields: `fde5c927`
- #64 required-nullable transport response fields: `8db8e257`
- #65 canonical CLI and MCP error boundary: `4c69540d`
- #66 release archive root-executable contract: `d37657a5`
- #67 direct and loopback-tunneled transport conformance: `e2420929`
- #68 shared parser and semantic error contracts: `73891fe1`
- #69 native-readiness admission truthfulness: `764b4c6f`
- #70 restart recovery and terminal-stop proof mappings: `67ce6856`
- #71 public-boundary liveness and WebSocket isolation mappings: `68e43416`
- #72 closed Turn-state vocabulary proof mappings: `23c8f329`
- #73 session stop public contract mappings (`i3f` and `5hb`; the `hav` mapping was later invalidated): `6c45f458`
- #74 direct HTTPS/WSS and established-tunnel event parity: `c7dbf556`
- #75 public Session and Turn revision values: `a0fdff96`
- #76 revision advancement and immutable bindings: `48651c86`
- #77 tunneled Host Daemon API authentication: `8de62bad`
- #78 terminal-state race invariants: `6f1b6123`
- #79 semantic release archive contract cases: `87e189c5`
- #80 deterministic active-Turn stop proof: `1ad7f30e`
- #81 npm promotion transaction contract catalog: `ff4e45d0`
- #82 archive-to-npm executable parity catalog: `76279f0f`
- #83 shared versioned-payload contract catalog: `8a317a90`
- #84 cross-boundary privacy-canary foundation: `c6201a91`

Historical checkpoint after PR #84: the maintenance no-mutation foundation was the only active implementation packet at that time.

## Baseline Verification

- Passed: `092` formatting, `at6` Clippy with warnings denied, `y6s` workspace tests, and `qqj` no manual CI facts.
- Passed: `jiw` locked release workspace build on the stable root target.
- PR #60 passed Ubuntu Rust, macOS Rust, and all npm jobs. Its Windows Rust job failed six existing `satelle-host` tests at a shared two-second scheduler deadline; the product diff did not touch runtime code.
- PR #61 passed all seven required checks. Greptile reviewed five files with zero comments, Codex reported no major issues on the merged head, and no review threads remained.
- PR #62 passed all seven required checks. Greptile completed without findings, Codex reported no major issues on the merged head, and no review threads remained.
- PR #63 passed all seven required checks. Greptile completed without findings, Codex reported no major issues on the merged head, and no review threads remained.
- PR #64 passed all seven required checks. Greptile completed without findings, Codex reported no major issues on the merged head, and no review threads remained.
- PR #65 passed all seven required checks. Greptile completed without findings, Codex reported no major issues on the merged head, and no review threads remained.
- PR #66 passed all six required CI jobs. Greptile completed without findings, Codex reported no major issues on head `229149e8`, and no review threads remained.
- PR #67 passed all six required CI jobs. Greptile completed without findings, Codex reported no major issues on head `6bef0304`, and no review threads remained.
- PR #68 passed all six required CI jobs. Greptile completed without findings, Codex reported no major issues on head `392b357b`, and no review threads remained.
- PR #69 passed all six required CI jobs. Greptile completed without findings, Codex reported no major issues on head `cd7ac313`, and no review threads remained.
- PR #70 passed all six required CI jobs. Greptile and Codex completed without findings on corrected head `e786512e`; both earlier P1 threads were resolved and outdated before merge.
- PR #71 passed all six required CI jobs. Greptile completed without findings, Codex reported no major issues on head `4413e7b9`, and no review threads remained.
- PR #72 passed all six required CI jobs. Greptile reached 5/5, Codex reported no major issues on corrected head `892978e3`, and all three valid Codex findings were fixed and resolved. The earlier macOS process-group test failure did not reproduce on the final head.
- PR #73 passed all six required CI jobs. Greptile reached 5/5, Codex reported no major issues on head `075b295f`, and no review threads remained. A later scheduling audit invalidated only its `hav` CLI proof; `i3f` and `5hb` remain proven.
- PR #74 passed all six required CI jobs. Codex reported no major issues on head `c276bb78`; Greptile retracted its sole P1 after rcgen source proved the IP-address SAN correct, and the resolved thread had no actionable finding.
- PR #75 passed all six required CI jobs. Greptile reached 5/5, Codex reported no major issues on head `593b163a`, and no review threads remained.
- PR #76 passed all six required CI jobs. Greptile reached 5/5, Codex reported no major issues on head `31405934`, and no review threads remained.
- PR #77 passed all six required CI jobs. Greptile reached 5/5, Codex reported no major issues on head `c3d40484`, and no review threads remained.
- PR #78 passed all six required CI jobs. Greptile reached 5/5, Codex reported no major issues on head `412d2257`, and no review threads remained; it merged at `6f1b6123`.
- PR #79 passed all six required CI jobs. Greptile reviewed seven files with zero comments, Codex reported no major issues on head `9d51deec`, CodeRabbit completed successfully, and all review threads were resolved; it merged at `87e189c5`.
- PR #80 passed all six required CI jobs. Greptile and CodeRabbit completed without findings, Codex reported no major issues on head `e031a3ac`, and no review threads remained; it merged at `1ad7f30e`.
- PR #81 passed all six required CI jobs. Greptile reached 5/5 and CodeRabbit passed final head `4e611bbf`; Codex reviewed head `1ec66506`, its valid finding was fixed and resolved, and its one final-head request did not respond. All review threads were resolved before merge at `ff4e45d0`.
- PR #82 passed all six required CI jobs. Greptile reached 5/5, CodeRabbit completed successfully, and Codex reported no major issues on final head `252a653a`; all review threads were resolved before merge at `76279f0f`. An intermediate Ubuntu reconnect test failed outside the two-file PR scope, then passed on the fresh final head without a CLI source change.
- PR #83 passed all six required CI jobs. Greptile reached 5/5, CodeRabbit completed successfully, and Codex reported no major issues on final head `f18e3068`; all review threads were resolved before merge at `8a317a90`. The first Windows CI attempt timed out in an existing transport worker-idle test outside the two-file packet, then a clean rerun passed the entire workspace without a transport source change. The exact timed-out test also passed natively on the final head under the preserved Windows 11 ARM64 lease.
- PR #84 passed all six required CI jobs. Greptile and CodeRabbit completed successfully, Codex reported no major issues on final head `9c458c3f`, and the one valid CodeRabbit audit-count finding was fixed and resolved before merge at `c6201a91`. A trivial outside-diff helper suggestion was declined because the six explicit prompt constructions define independently named privacy fixtures and expose the exact input under test.
- The deterministic `hav` packet passed formatting; five exact CLI-boundary invocations; all 77 CLI integration tests; 223 Host tests with one ignored; Host and CLI no-default checks; warnings-denied Host and CLI Clippy across all targets and features; and the complete workspace suite with 578 passed and two ignored across 17 suites. Windows 11 independently passed the exact CLI-boundary contract and the same warnings-denied Host and CLI Clippy gate on head `e031a3ac`.
- A Git-backed active-branch `facts check --tags 'verification or release-gate'` completed with 9 passed commands, zero failures, and 29 manual facts. This proves every existing command in the tagged set but does not verify or implement any manual fact.
- A fresh broad Git-backed `facts check` on then-current main `1ad7f30e` completed with 31 passed commands, zero failures, and 2,070 manual facts. This closes the historical `hav` failure from post-PR-#78 main without falsely crediting any unmapped manual fact.
- The npm promotion catalog passed formatting, facts syntax validation, its locked `satelle-test-contract` suite, warnings-denied crate Clippy, the complete locked workspace suite with 582 passed and two ignored across 17 suites, and warnings-denied workspace Clippy across all targets and features. Windows 11 independently passed the locked 8-test `satelle-test-contract` suite and the same warnings-denied crate Clippy gate on corrected code head `36d76fcc`. The catalog covers the complete promotion and rollback-recovery writer matrix plus identity and value mismatches across candidate packages, every prior-`latest` field, progress rows, and final rereads. All valid findings were addressed before PR #81 merged.
- The archive-to-npm parity catalog passed formatting, facts syntax validation, its locked 13-test `satelle-test-contract` suite, and warnings-denied crate Clippy. Windows 11 independently passed the same locked suite and Clippy gate on exact code head `6c043f25`. Its seven private cases cover the matching six-platform graph, an accepted order permutation, digest mismatch, missing corresponding package, same-cardinality wrong identity correspondence, and both directional archive/package version mismatches; one independent public-catalog reference-model test executes all seven cases. Codex's missing-release-archive suggestion remains owned by completeness fact `fr9`; its candidate-only mismatch suggestion was addressed by removing the unused outer candidate field because authoritative full-graph version alignment belongs to `cmb`. Fact `h9h` remains unmapped until a production archive/npm adapter exists.
- The shared versioned-payload catalog passed formatting, facts syntax validation, its locked 18-test `satelle-test-contract` suite, and warnings-denied crate Clippy. Windows 11 independently passed the same locked suite and Clippy gate on exact code head `b652fd98`. Its 15 private cases cover JSON and NDJSON framing, exact schema-version tokens, required-field presence including a nullable value, enum-token types and membership, opaque carrier equality, top-level object shape, and malformed or invalid later NDJSON records; one independent public-catalog reference-model test executes all 15 cases. Unknown-field and process-versus-transport policy remain undefined, and fact `4ze` remains unmapped under GAP-011.
- The privacy-canary foundation passed formatting, direct facts syntax validation, 20 locked shared-contract tests, all 77 CLI integration tests, 223 Host tests with one intentional child-fixture ignore, all 66 HTTP/WebSocket transport tests, the complete all-feature workspace suite with 594 passed and two ignored across 17 suites, and workspace-wide all-target/all-feature Clippy with warnings denied. One independent audit found that composed prompt strings could miss a marker-only leak; every CLI, HTTP, and WebSocket caller now checks the prompt marker and secret independently. Raw WebSocket frames are checked before deserialization, and the redundant post-deserialization check was removed. Fact `a5u` remains unmapped because the setup Action Ledger does not exist.
- Windows 11 independently fully resynced all 202 repository files from exact pushed code head `64065ac9`; passed all 20 locked shared-contract tests; passed the targeted real CLI, Host runtime, Host storage, WebSocket event, HTTP logs, HTTP session, and HTTP idempotency-conflict privacy tests; and passed all-target/all-feature Clippy with warnings denied for `satelle-test-contract`, `satelle-cli`, `satelle-host`, and `satelle-transport`.
- The maintenance no-mutation foundation passed intentional-red proofs for an added sandbox path, a permission-only change, and a transient create/delete mutation; the clean production setup dry-run proof; all 24 shared-contract tests; all 77 CLI integration tests; the complete all-feature workspace suite with 598 passed and two ignored across 21 suites; and workspace-wide all-target/all-feature Clippy with warnings denied. The native notify watcher uses a helper-owned causal barrier, fails closed on lost events, ignores only read/access-time events, does not follow symlinks, and removes its helper-owned barrier during unwinding. The production dry-run proof asserts the exact five daemon path overrides in the public report. Independent follow-up audits approved the access-metadata and watcher designs. Windows 11 independently fully resynced all 202 repository files from exact pushed code head `f65de61d`; passed all 24 locked shared-contract tests including the permission, transient-mutation, and barrier-unwind regressions; passed the exact production setup dry-run contract with 76 unrelated CLI tests filtered; and passed all-target/all-feature Clippy with warnings denied for `satelle-test-contract` and `satelle-cli`. Fact `mg1` remains unmapped because repair, Host update, self-update remotes, and doctor fix do not yet expose real complete dry-run boundaries.

CI hygiene: each npm matrix job currently warns that pinned `pnpm/action-setup` targets deprecated Node.js 20 and is forced onto Node.js 24. This is non-blocking but should be removed before GitHub drops the compatibility path.

The first `jiw` attempt used a stale shared target beneath a workspace that disappeared during the build. The exact release command was rerun against the stable root target and completed successfully. This was an environment-path failure, not a source failure.

Facts CLI 0.6.0 discovers a project through `.git` and is not aware of Jujutsu-only secondary workspaces. Run canonical `facts list` and `facts check` only after integrating the branch into the root workspace; use `facts lint --file <absolute-path>` for direct secondary-workspace syntax validation.

## Branch Disposition

- `feat/capability-truthfulness`: paused until Phases 1 and 2 permit dependent feature work.
- `docs/public-readme-security`: paused for the release and documentation lane.
- `spec/api-token-administration`: reference only; its fact-only diff conflicts with the freeze.
- `feat/provider-probe-surface`: reference only; re-scope fresh after prerequisite phases.
- `fix/macos-storage-lock-handoff`: already represented on main; retire as a candidate.
- `feat/windows-codex-app-policy`: no effective diff; retire as a candidate.

Do not delete preserved branches as part of orchestration cleanup.

## Phase Gates

1. Phase 0: freeze inventory, add this checkpoint and the spec-gap ledger, review, merge, and sync main.
2. Phase 1: complete the verification harness before feature implementation. Map existing verification facts only to production boundaries, add missing cross-process checks, and build every currently possible shared semantic catalog and cross-platform test foundation without falsely implementing blocked facts.
3. Phase 2: complete all remaining contract facts before dependent feature work. Independent release-infrastructure work may start only after the Phase 1 harness is operational and only when its contracts are stable.
4. Phase 3: after Phases 1 and 2, run three non-overlapping lanes with at most one unmerged branch per lane:
   - Lane A: Host, Bridge, Sessions, Storage, then Computer Use.
   - Lane B: CLI, Errors, then Config.
   - Lane C: Release, Docs, npm/install, then attestation.
5. Phase 4: run real-environment verification and release gates, then audit the definition of done.

PRs may develop in parallel, but reviews, merges, and main synchronization are serialized. Each major work unit uses a fresh semantic branch, relevant tests, a push with Residue hooks intact, a PR, Greptile and Codex review when available, fixes for valid findings, merge, and a main sync before the next merge.

## Next Eligible Work

| Queue | Work unit | Eligibility |
| --- | --- | --- |
| Contract | Shared CLI parser and semantic error boundary (`4ze` prerequisite) | Merged in PR #65; GAP-009 numeric normalization remains out of scope |
| Verification | Shared parser and semantic error assertion crate (`zox`) | Merged in PR #68; GAP-009 numeric normalization remains out of scope |
| Verification | Shared release archive contract harness (`3ha`, `298`) | Merged in PR #79; records only semantic cases and private expected outcomes, while both facts remain unmapped until a production validator adapter exists |
| Verification | Deterministic active-Turn stop proof (`hav`) | Merged in PR #80; the exact CLI-boundary command is deterministic on Linux and Windows 11 |
| Verification | npm promotion transaction catalog (`hz8`, `kn9`, `jxb`, `kgm`, `bqi`, `343`, `lud`) | Merged in PR #81; records private semantic outcomes while all seven facts remain unmapped until a production workflow adapter and GAP-012 resolution exist |
| Verification | Archive-to-npm executable parity (`h9h`) | Merged in PR #82; records seven private digest-parity cases and an independent public-catalog execution test while `h9h` remains unmapped until a production archive/npm adapter exists |
| Verification | Shared versioned-payload assertions (`4ze` foundation) | Merged in PR #83; records exact JSON, NDJSON, field, enum, and opaque-carrier cases while `4ze` remains unmapped until GAP-011 is resolved |
| Verification | Cross-boundary privacy canaries (`a5u` foundation) | Merged in PR #84; one raw-byte assertion now covers real CLI, HTTP, WebSocket, Host, and storage boundaries while `a5u` remains unmapped until the setup Action Ledger exists |
| Verification | Maintenance no-mutation assertions (`mg1` foundation) | Linux and native Windows 11 watcher proof complete; PR review gates remain, and the fact stays unmapped until repair, Host update, self-update remotes, and doctor fix expose complete real dry-run boundaries |
| Verification | Real Host lifecycle transcript (`l6a`, `chk` foundation) | Add reusable fresh-process and daemon-restart assertions without the fake adapter; keep both facts unmapped until the missing product surfaces exist and real Windows plus macOS Host runs pass |
| Lane A | Direct and SSH-tunneled transport conformance (`imt`) | Merged in PR #67; the deterministic loopback TCP forward represents the established tunnel boundary |
| Lane A | Native-readiness admission truthfulness (`pe1`) | Merged in PR #69; deterministic admission proof only, not real native readiness execution |
| Lane A | Restart recovery and terminal-stop proofs (`zpt`, `kqy`) | Merged in PR #70; maps existing mutation-sensitive exact tests without duplicate test growth |
| Lane A | Public-boundary liveness and WebSocket isolation (`kyc`, `q82`) | Merged in PR #71; maps existing real-boundary exact tests |
| Lane A | Closed Turn-state vocabulary (`0hy`, `ftq`) | Merged in PR #72; the exact serialized-name and terminal-partition proof is compiler-exhaustive over `TurnState` |
| Lane A | Session stop public contract (`i3f`, `5hb`) | Merged in PR #73; its later-audited `hav` CLI proof is invalid and queued separately above |
| Lane A | Direct HTTPS/WSS and established-tunnel event parity (`0jf`) | Merged in PR #74; one shared exact test covers real direct TLS plus the established-tunnel boundary |
| Lane A | Public Session and Turn revision values (`g60`) | Merged in PR #75; the existing allowlist test now checks both live revision values |
| Lane A | Revision advancement and immutable bindings (`lwp`, `ksl`) | Merged in PR #76; existing Session tests now prove exact advancement, preservation, and immutable bindings |
| Lane A | Tunneled Host Daemon API authentication (`cr4`) | Merged in PR #77; one exact test proves negative HTTP and WebSocket authentication through the established-tunnel boundary |
| Lane A | Terminal-state race invariants (`hjz`, `t10`, `sp8`) | Merged in PR #78; exact core and Host tests prove terminal immutability and compare-and-set race outcomes |
| Lane A | Public Session admission boundary (`f8i`, `wji`, `xxp`) | Audited first Lane A packet, but blocked until Phases 1 and 2 are complete |
| Lane B | Secret-file normalization (`6kb`) | Audited first Lane B packet, but blocked until Phases 1 and 2 are complete |
| Release infrastructure | Production archive validator (`3ha`, `298`) | Blocked until every Phase 1 harness foundation is operational; then it may proceed during Phase 2 under the independent-release exception and must validate real tar.gz and ZIP bytes through the production release path before mapping either fact |

The 29 remaining verification/release-gate facts are fully routed as follows:

- Harness foundations that can be authored now while the facts remain unmapped: `298`, `h9h`, `hz8`, `kn9`, `jxb`, `kgm`, `bqi`, `343`, `lud`, `4ze`, `mg1`, `a5u`, `l6a`, and `chk`.
- Blocked by specified product surfaces, platforms, or recorded gaps: `eew`, `kh7s`, `tzd`, `jcg`, `n9x`, `x2z`, `jlb`, `9qc`, `tbu`, `q2j`, `eeh`, `sdl`, and `omu`.
- Final real-environment or release evidence: `l0l` and `m42`.

Fact `hav` is outside this tagged union and now has deterministic proof merged in PR #80; it no longer occupies the remaining Phase 1 foundation queue.

Provision and validate Ubuntu as a Controller and Windows 11 as a native Computer Use Host for `setup --verify`, `run`, `status`, and `stop` flows as part of Phase 1. Linux is not a frozen native Computer Use Host Platform. A real macOS native Host journey is also required before the cross-platform lifecycle facts can be implemented.

Linux Phase 1 provisioning created one lease, but provider resolution failed before SSH, checkout sync, or remote command execution. The exact lease was released cleanly and zero Linux leases remain active. Evidence: `/home/ubuntu/.codex/artifacts/satelle-phase1-linux-20260715/transcript.txt`, SHA-256 `f40ac4e2b8be8449ba73d4c824a28dfd34374192e059553ff790b8876bad01c8`.

Windows 10 testing is prohibited. Preserved claim `cbx_32b0a6523bb2` remains leased; its latest metadata reports `ready=false` and `has_host=true`, while the same invocation successfully reached SSH, fully resynced the checkout, and ran both proof commands. Although the broader provider inventory commands previously exited 255, repeated direct full resyncs and native Windows 11 runs prove current SSH proxy reachability; the latest versioned-payload proof fully resynced 202 files and passed the locked 18-test suite plus warnings-denied crate Clippy on exact code head `b652fd98`. Release-harness source commit `e5c39ac3` was fully resynced as 202 repository files on 2026-07-15; its locked `satelle-test-contract` suite passed 4/4 tests and its all-targets Clippy gate passed with warnings denied. The Windows 11 Pro ARM64 guest now has an 80 GiB system disk, signed ARM64 C++ Build Tools plus clang-cl 19.1.5, Rust 1.97.0 with rustfmt and Clippy, Node 18.20.8, and npm 10.8.2. The warnings-denied locked release build passed and installed `D:\Satelle\bin\satelle.exe` with SHA-256 `ac946cae110239190bb962b9d61229c87add6ba6a36486275c6f91d3de55e764`; `satelle 0.1.0` and `satelle paths --json` passed. `satelle setup --verify` is absent and returned typed `invalid-usage` with exit 64. The noninteractive setup command returned `satelle.setup.v1` status `planned`, no applied actions, and `mutated: false`; run, status, and stop returned typed input errors because no prompt or Session identifier was invented. The claim remains leased and directly reachable. Scrubbed evidence is stored under `/home/ubuntu/.codex/artifacts/satelle-phase1-windows-20260715/`; the checksum-manifest SHA-256 is `101b7987c401f48ec29dce7dccb68ab35f2c1cf08cf0ccdd4dd4565d0a65efc1`.

The later privacy-canary run supersedes the stale `ready=false` inventory snapshot above: claim `cbx_32b0a6523bb2` was ready and reachable, fully resynced all 202 files from exact code head `64065ac9`, passed all seven targeted boundary tests, and passed the four-package warnings-denied Clippy gate. The claim remains leased and must not be released.

The final maintenance no-mutation Windows run reconfirmed that preserved claim `cbx_32b0a6523bb2` is ready and reachable. It fully resynced all 202 files from exact pushed code head `f65de61d`, then passed the 24-test shared-contract suite including the permission-only, transient-mutation, and barrier-unwind regressions, the exact production setup dry-run contract with all five daemon path overrides asserted, and the two-package warnings-denied Clippy gate under the signed ARM64 Visual Studio environment. The claim remains leased and must not be released.

## Known Blockers

- Most verification and release-gate facts do not yet have executable commands.
- Fact `hav` now has deterministic CLI-boundary proof on merged main and passed Linux, Windows 11, and CI validation in PR #80.
- Native SSH and end-to-end lifecycle coverage is incomplete.
- Archive, installer, publication, and attestation gates are incomplete.
- No production release archive validator exists. The shared `3ha` and `298` cases cannot earn fact commands or `@implemented` tags until one production adapter invokes the same validator used by release automation.
- Linux provisioning is blocked at provider resolution before SSH. The trusted provider calls unfiltered `docker port`, receives `22/tcp -> ...` lines, then incorrectly parses for lines beginning `0.0.0.0:` and exits under `pipefail`. The durable provider source differs from the deployed script, so repair requires reconciling the active extended provider into template-boxes automation, testing filtered `docker port <container> 22/tcp` plus the missing-mapping diagnostic, and deploying it. The failed lease was released and zero Linux leases are active; this external infrastructure change is not part of the Satelle branch.
- Windows 11 build and basic CLI verification now pass on the preserved ARM64 claim, but lifecycle verification remains incomplete. The required `setup --verify` surface is not implemented; Host Daemon installation, native Computer Use acceptance, provider authentication, configured-Host execution, and a real prompt-to-Session run/status/stop journey remain unverified. Windows 10 remains prohibited.
- Real lifecycle acceptance is blocked by missing product boundaries: `satelle host trust` is absent, production setup only plans without mutation, direct setup/doctor and every SSH transport operation are unimplemented, production native Computer Use execution is unimplemented, and persistent Host start/stop/restart are unimplemented. Current fake-adapter journeys cannot satisfy `l6a` or `chk`.
- Ubuntu can prove Controller installation, SSH, and Controller-to-Host behavior after the provider repair, but it cannot substitute for the required native macOS Host acceptance journey.
- GAP-012 leaves npm promotion cancellation semantics, the complete record-state vocabulary, durable serialization, and public publication-failure behavior undefined. The abstract promotion catalog may proceed, but all seven facts must remain unmapped until a production workflow adapter exists and the missing operational contract is resolved.
- Fact `4ze` conflates process channels with HTTP and WebSocket payloads. GAP-011 records the required scope decision; do not tag `4ze` implemented until the contract defines native transport equivalents or limits itself to process surfaces.
- Fact `6s7` remains unmapped. Production discovery uses the current operating system and direct probes without a synthetic cross-platform seam, so deterministic injection would be tautological; proof requires a real multi-platform harness or a justified production discovery seam.

Actionable review findings: PR #81, PR #82, PR #83, and PR #84 have no unresolved findings. The privacy packet's independent P1 prompt-marker finding and CodeRabbit audit-count finding were fixed, its redundant typed WebSocket assertion was removed, and Greptile, CodeRabbit, and Codex completed on the final head. PR #82's Codex threads are resolved: release-artifact completeness stays with `fr9`, removing the unused outer candidate field avoids importing `cmb` full-graph alignment into `h9h`, and an accepted package-order permutation now rejects zip-by-position adapter design without inventing order semantics. CodeRabbit's historical-main wording and public-catalog execution findings are fixed and resolved; Greptile, CodeRabbit, and Codex all completed on the final head. PR #79's catalog models zero, one, or multiple executables; covers symmetric Unix and Windows root-layout failures plus absent Linux libc identity; distinguishes validator rejection from harness failure; and limits its unit tests to generic driver behavior. The loopback shutdown finding was fixed with a deterministic failed-upstream regression before PR #67, and both automated reviews completed without further findings.

Stop a work packet on a fact conflict, overlapping ownership, unavailable required platform, or repeated blocker. If a subagent stalls on additional safety checks, terminate it immediately and retry a smaller isolated packet.

# Production Readiness Orchestration State

This file is the tracked handoff for the production-readiness program. Update it after every merge and before handing work to another session. The `.facts` file remains the product contract; this file records execution state only. Ignored state under `.workflow/satelle-production-readiness/` is stale history and is not an operational authority.

## Freeze Baseline

- Phase: 1 - verification harness
- Latest main: `fde5c927d4efa6aeb42a6b717bf9933357c0d88e`
- Freeze base: `a4bbea5a3aaf7df22c175587be26d072f25cfd03`
- Fact sheet SHA-256: `c95b7090372cc107bed9fb4752469fff4ae7798d945563f91aa57e5ab2fcb03c`
- Facts CLI: `facts 0.6.0`
- Fact write policy: do not add facts, contracts, or scope. Phase 1 may add executable command mappings to existing verification facts. Record undefined behavior in `.spec-gaps.md` instead of inventing requirements.

## Frozen Inventory

| Set | Total | Remaining |
| --- | ---: | ---: |
| All facts | 2,101 | 1,465 specs |
| Implemented | 636 | - |
| Spec | 1,703 | 1,465 |
| Draft | 0 | 0 |
| Contract | 223 | 71 |
| Verification | 38 | 37 |
| Release gate | 23 | 23 |
| MVP | 1,783 | 1,151 |
| Later | 318 | 314 |

`@spec` and `@implemented` overlap on 238 facts. Remaining specs consist of 1,151 MVP facts and 314 later facts. Tag totals use facts CLI semantics and include nine mapping-form `tags: [...]` entries.

Current Phase 1 state: 641 implemented facts, 1,460 specs remaining, 32 verification facts remaining, 21 release gates remaining, 1,140 MVP facts remaining, and 320 later facts remaining. Six token-administration facts moved from MVP to later in `fdaf6cd`; no product implementation was removed.

## Current Ownership

| Lane | Owner | Active work | Unmerged branch |
| --- | --- | --- | --- |
| Orchestration | Root session | Phase 1 checkpoint and serial merge queue | None |
| Contract writer | `transport_nullable_writer` | Required-nullable transport response fields (`4ze` prerequisite) | `feat/transport-nullable-wire-contract` |
| Contract review | `transport_nullable_review` | Independent Serde and test-design review | None (read-only) |
| Contract prep | Root session | CLI error-boundary packet | None (read-only) |
| A: host and runtime | Unassigned | Blocked by Phase 1 completion and Phase 2 | None |
| B: CLI and configuration | Unassigned | Blocked by Phases 1 and 2 | None |
| C: release and distribution | Unassigned | Blocked by Phases 1 and 2 | None |

Only the active writer may edit. Preparation and review lanes are read-only so `.facts` remains single-writer and independently prepared packets do not create cross-branch conflicts.

## Merge Ledger

- #54 macOS process cleanup and storage lock work: `30b943bf`
- #56 retain Codex stdout through cleanup: `1135029a`
- #55 npm, pnpm, and Bun one-shot distribution facts: `c2f8d7d7`
- #57 validate storage integrity before migrations: `2f0b3d61`
- #58 event control context validation: `a4bbea5a`
- #59 production-readiness fact freeze and orchestration checkpoint: `57435efd`
- #60 map verification facts to executable checks: `477d5648`
- Separate authorized session resolved GAP-001 and GAP-002: `fdaf6cde`
- Separate authorized session updated this ledger for those resolutions: `db22843f`
- #61 deterministic runtime synchronization and macOS group reaping: `d0ec268e`
- #62 cross-process Control Lease exclusion: `1b8eb4fa`
- #63 required-nullable core wire fields: `fde5c927`

Open PRs: none. PR #63 passed all required checks and both automated review lanes before merging.

## Baseline Verification

- Passed: `092` formatting, `at6` Clippy with warnings denied, `y6s` workspace tests, and `qqj` no manual CI facts.
- Passed: `jiw` locked release workspace build on the stable root target.
- PR #60 passed Ubuntu Rust, macOS Rust, and all npm jobs. Its Windows Rust job failed six existing `satelle-host` tests at a shared two-second scheduler deadline; the product diff did not touch runtime code.
- PR #61 passed all seven required checks. Greptile reviewed five files with zero comments, Codex reported no major issues on the merged head, and no review threads remained.
- PR #62 passed all seven required checks. Greptile completed without findings, Codex reported no major issues on the merged head, and no review threads remained.
- PR #63 passed all seven required checks. Greptile completed without findings, Codex reported no major issues on the merged head, and no review threads remained.

The first `jiw` attempt used a stale shared target beneath a workspace that disappeared during the build. The exact release command was rerun against the stable root target and completed successfully. This was an environment-path failure, not a source failure.

Facts CLI 0.6.0 discovers a project through `.git` and is not aware of Jujutsu-only secondary workspaces. Run canonical `facts list` and `facts check` only after integrating the branch into the root workspace; use `facts lint --file <absolute-path>` for direct secondary-workspace syntax validation.

## Branch Disposition

- `feat/capability-truthfulness`: paused until Phases 1 and 2 permit dependent feature work.
- `docs/public-readme-security`: paused for the release and documentation lane.
- `spec/api-token-administration`: reference only; its fact-only diff conflicts with the freeze.
- `feat/provider-probe-surface`: reference only; re-scope fresh after prerequisite phases.
- `fix/macos-storage-lock-handoff`: already represented on main; retire as a candidate.
- `feat/windows-codex-app-policy`: no effective diff; retire as a candidate.

Do not delete preserved branches as part of orchestration cleanup.

## Phase Gates

1. Phase 0: freeze inventory, add this checkpoint and the spec-gap ledger, review, merge, and sync main.
2. Phase 1: build the verification harness first. Map existing verification facts to commands, add missing cross-process and archive checks, then validate the current build on Ubuntu and Windows 11.
3. Phase 2: implement all 71 remaining contract facts before dependent feature work.
4. Phase 3: run three non-overlapping lanes with at most one unmerged branch per lane:
   - Lane A: Host, Bridge, Sessions, Storage, then Computer Use.
   - Lane B: CLI, Errors, then Config.
   - Lane C: Release, Docs, npm/install, then attestation.
5. Phase 4: run real-environment verification and release gates, then audit the definition of done.

PRs may develop in parallel, but reviews, merges, and main synchronization are serialized. Each major work unit uses a fresh semantic branch, relevant tests, a push with Residue hooks intact, a PR, Greptile and Codex review when available, fixes for valid findings, merge, and a main sync before the next merge.

## Next Eligible Work

| Queue | Work unit | Eligibility |
| --- | --- | --- |
| Contract | Required-nullable transport response fields (`4ze` prerequisite) | In progress; does not claim the broad `4ze` fact |
| Verification | Archive root-executable validator (`3ha`) | Needs a Phase 1 policy decision because `3ha` lacks the `verification` tag |
| Verification | Direct and SSH-tunneled transport conformance (`imt`) | Blocked until the established-tunnel boundary is confirmed; do not substitute a loopback proxy for native SSH silently |
| Lane A | First contract cluster by owned host/runtime files | After Phase 1 is operational |
| Lane B | Closed CLI schema and error matrices | During Phase 1 after command mapping |
| Lane C | Archive and installer validators | During Phase 1 after command mapping |

Provision and validate Ubuntu and Windows 11 for native `setup --verify`, `run`, `status`, and `stop` flows as part of Phase 1.

Windows 10 testing is prohibited. Windows 11 was reported available by the user but has not yet been verified in this phase. Crabbox provisioning is deferred until Phase 1, when it is required by the objective.

## Known Blockers

- Most verification and release-gate facts do not yet have executable commands.
- Native SSH and end-to-end lifecycle coverage is incomplete.
- Archive, installer, publication, and attestation gates are incomplete.
- Real Windows 11 validation is blocked by missing `setup --verify`, Host trust/lifecycle, configured-Host execution, and native Computer Use execution. Windows 10 remains prohibited.

Actionable review findings: none.

Stop a work packet on a fact conflict, overlapping ownership, unavailable required platform, or repeated blocker. If a subagent stalls on additional safety checks, terminate it immediately and retry a smaller isolated packet.

# This private workspace builds the website from TypeScript and runs its tests
# in prebuild. Keep build dependencies even when NODE_ENV=production or an
# install command requests --omit=dev; only the static export is deployed.
include=dev
